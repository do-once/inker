/**
 * @inker/compute-worker — 计算策略
 *
 * @deprecated 整个包已废弃。计算职责已内聚到各 RenderAdapter 内部。
 * 请使用 @inker/render-canvas 或 @inker/render-offscreen 替代。
 */

export { MainThreadStrategy } from './main-thread.strategy'
export { WorkerStrategy } from './worker.strategy'
