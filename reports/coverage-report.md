# Inker E2E 测试覆盖度报告

> 生成时间：2026-04-23
> 测试框架：Playwright 1.59.1 + Chromium（headed 模式，slowMo: 300ms）
> 测试结果：**89/89 通过 (100%)**
> 发现缺陷：**1 个**（已记录至 issues/）

## 覆盖度总览

| 功能域 | 测试文件 | 用例数 | 通过 |
|--------|----------|--------|------|
| 绘制路径 | drawing.spec.ts | 11 | 11 |
| 橡皮擦与编辑 | eraser-edit.spec.ts | 10 | 10 |
| 视口与缩放 | viewport.spec.ts | 10 | 10 |
| 导入导出与回放 | export-playback.spec.ts | 10 | 10 |
| 渲染器与场景切换 | renderer-recognition.spec.ts | 11 | 11 |
| 笔刷参数全覆盖 | brush-params.spec.ts | 10 | 10 |
| 交叉场景 | cross-scenario.spec.ts | 13 | 13 |
| 面板与识别场景 | panel-recognition.spec.ts | 14 | 14 |
| **总计** | **8 个文件** | **89** | **89** |

## 按功能域详细覆盖

### 1. 绘制路径 (drawing.spec.ts — 11 用例)

| 操作路径 | 用例 |
|----------|------|
| 钢笔绘制 | 绘制一笔后笔画数加一 |
| 马克笔绘制 | 绘制一笔后笔画数加一 |
| 铅笔绘制 | 绘制一笔后笔画数加一 |
| 连续多笔 | 绘制三笔后笔画数为三 |
| 工具切换后绘制 | 钢笔与马克笔各一笔共两笔 |
| 样式面板-颜色修改 | 修改颜色后绘制不崩溃 |
| 样式面板-大小修改 | 修改大小滑块后绘制不崩溃 |
| 笔刷参数-模拟压感 | 勾选后绘制不崩溃 |
| 笔刷参数-缓动函数 | 修改后绘制不崩溃 |
| 笔刷参数-官网默认 | 点击后绘制不崩溃 |
| 默认工具 | 页面加载后钢笔已激活 |

### 2. 橡皮擦与编辑操作 (eraser-edit.spec.ts — 10 用例)

| 操作路径 | 用例 |
|----------|------|
| 橡皮擦基础 | 擦除后笔画数减少 |
| 橡皮擦空画布 | 擦除不崩溃且笔画数保持 0 |
| 撤销单笔 | 撤销后笔画数为 0 |
| 撤销多笔 | 撤销两次后笔画数为 1 |
| 重做 | 撤销后重做恢复笔画数为 1 |
| 撤销按钮禁用态 | 空画布时撤销按钮 disabled |
| 重做按钮禁用态 | 空画布时恢复按钮 disabled |
| 撤销后新绘制清除重做栈 | 绘制新笔后恢复按钮 disabled |
| 清空画布 | 点击清空后笔画数为 0 |
| 清空后撤销恢复 | 清空后撤销笔画数恢复为 3 |

### 3. 视口与缩放 (viewport.spec.ts — 10 用例)

| 操作路径 | 用例 |
|----------|------|
| 状态栏放大 | 点击加号后缩放值大于100% |
| 状态栏缩小 | 点击减号后缩放值小于100% |
| 连续放大 | 点击三次加号缩放值递增 |
| 面板放大缩小 | 面板内加减号改变缩放值 |
| 适应画布 | 绘制笔画后点击适应画布不崩溃 |
| 重置100% | 放大后点击100%恢复到100% |
| 平移模式切换 | 点击后按钮文本变为平移中且带active |
| 平移模式退出 | 进入平移模式后再次点击退回平移模式文本 |
| 滚轮缩放 | 在画布上滚轮后缩放值变化 |
| 缩放后绘制正常 | 放大后绘制笔画数加一 |

### 4. 导入导出与回放 (export-playback.spec.ts — 10 用例)

| 操作路径 | 用例 |
|----------|------|
| 导出 PNG | 绘制笔画后触发下载事件 |
| 导出 JSON | 绘制笔画后触发下载事件 |
| 导出 JSON 内容有效 | 包含 operations 或 strokes 字段 |
| 空画布导出 JSON | 不崩溃 |
| 导入 JSON | 笔画数恢复 |
| 播放按钮 | 点击后出现暂停和停止按钮 |
| 暂停与继续 | 暂停后出现继续按钮，继续后出现暂停按钮 |
| 停止回放 | 点击停止后恢复初始态只有播放按钮 |
| 速度切换 | 点击 2x 后该按钮获得 active class |
| 进度条更新 | 播放后进度百分比大于 0% |

### 5. 渲染器与场景切换 (renderer-recognition.spec.ts — 11 用例)

| 操作路径 | 用例 |
|----------|------|
| 默认 Canvas 渲染器 | 页面加载后 Canvas tab 带 active |
| 切换到 SVG | SVG tab 带 active，其他不带 |
| 切换到 Offscreen | Offscreen tab 带 active |
| 切换渲染器后绘制 | 切换到 SVG 后绘制笔画数加一 |
| 对比模式开启 | 按钮带 active，3 个 tab 变 disabled |
| 对比模式关闭 | 再次点击后按钮不带 active，tab 可用 |
| 切换到识别场景 | URL 变为 /#/recognition |
| 识别场景工具栏 | 只有钢笔/铅笔/橡皮 3 个工具按钮 |
| 识别场景绘制 | 绘制一笔后笔画数加一 |
| 识别参数面板 | 展开后 checkbox 和 slider 可见 |
| 切换回基础场景 | URL 变为 /#/basic，4 个工具按钮恢复 |

### 6. 笔刷参数全覆盖 (brush-params.spec.ts — 10 用例)

| 操作路径 | 用例 |
|----------|------|
| 透明度滑块 | 修改后绘制正常 |
| 细化滑块 | 调整后绘制正常 |
| 平滑滑块 | 调整后绘制正常 |
| 流线滑块 | 调整后绘制正常 |
| 起始渐细滑块 | 调整后绘制正常 |
| 结束渐细滑块 | 调整后绘制正常 |
| 起始端帽 | 勾选后绘制正常 |
| 结束端帽 | 勾选后绘制正常 |
| 多参数组合 | 同时调整多个参数后绘制正常 |
| 缓动函数逐个切换 | 前 5 个缓动函数都能绘制 |

### 7. 交叉场景 (cross-scenario.spec.ts — 13 用例)

| 操作路径 | 用例 |
|----------|------|
| 绘制→缩放→再绘制→撤销 | 撤销只回退一步，缩放保持 |
| 绘制→橡皮擦→撤销擦除 | 撤销橡皮擦操作恢复笔画 |
| 多工具切换绘制→全部撤销→全部重做 | 3 种笔刷各画一笔，撤销到 0 重做回 3 |
| 清空→撤销清空→再清空 | 清空可撤销，二次清空正常 |
| 绘制→导出JSON→清空→导入→回放 | 完整导出-导入-回放流程 |
| 导出PNG→缩放后导出PNG | 两次导出都成功 |
| SVG→绘制→导出→Canvas→导入 | 跨渲染器导出导入正常 |
| 对比模式下绘制→退出 | 对比模式下绘制计数正常 |
| 基础→识别→切回 | 场景切换不丢数据 |
| 识别场景撤销+清空 | 识别场景编辑操作完整 |
| 平移模式→绘制→退出→绘制 | 平移/绘制模式切换正常 |
| 缩放到极小→绘制→适应画布 | 极小缩放下绘制不崩溃 |
| 连续缩放到极大→重置100% | 极大缩放不崩溃，可重置 |

### 8. 面板与识别场景 (panel-recognition.spec.ts — 14 用例)

| 操作路径 | 用例 |
|----------|------|
| 折叠面板→展开面板 | 面板折叠/展开状态切换正常 |
| 手风琴切换 | 样式/笔刷参数/缩放/回放逐个展开 |
| 折叠已展开的手风琴节 | 折叠后内容隐藏 |
| 折叠面板后绘制 | 面板折叠不影响绘制 |
| 自动模式 checkbox | 切换自动模式正常 |
| 分组间隔滑块 | 调整到 500ms 正常 |
| 平移到包围盒原点 checkbox | 切换正常 |
| 自动模式下产生数据 | 绘制后等待自动识别 |
| 识别场景铅笔绘制 | 铅笔工具在识别场景正常 |
| 识别场景橡皮擦 | 橡皮擦在识别场景正常 |
| **导出JSON无响应（缺陷）** | 确认按钮点击无效 |
| 识别场景导出PNG | PNG 导出正常 |
| **缩放按钮无响应（缺陷）** | 确认按钮点击无效 |
| **导入JSON无响应（缺陷）** | 确认按钮可见但无功能 |

## 发现的缺陷

| Issue | 优先级 | 描述 | 位置 |
|-------|--------|------|------|
| [0423-2020](../issues/0423-2020-识别场景状态栏操作未注册.md) | P2 | 识别场景状态栏 exportJSON/importJSON/zoomIn/zoomOut 未注册 | RecognitionScene.vue:107-111 |

## UI 组件覆盖矩阵

| 组件 | 交互元素 | 覆盖状态 |
|------|----------|----------|
| TopBar | 场景选择、4 个工具按钮 | 全部覆盖 |
| StatusBar | 撤销/恢复/清空/导出PNG/导出JSON/导入JSON/缩放+/- | 全部覆盖（含识别场景缺陷检测） |
| FloatingPanel | 展开/折叠/手风琴切换 | 全部覆盖 |
| StylePanel | 颜色选择器、大小滑块、透明度滑块 | 全部覆盖 |
| FreehandPanel | 细化/平滑/流线/缓动/模拟压感/起始渐细/起始端帽/结束渐细/结束端帽/官网默认 | 全部覆盖（含多参数组合） |
| ZoomPanel | 放大/缩小/适应画布/100%/平移模式 | 全部覆盖 |
| PlaybackPanel | 播放/暂停/继续/停止/速度切换/进度条 | 全部覆盖 |
| RendererTabs | Canvas/SVG/Offscreen/对比模式 | 全部覆盖 |
| RecognitionParams | 自动/手动模式、分组间隔、导出格式、包围盒原点 | 全部覆盖 |

## 运行命令

```bash
# 全部测试（headed 模式）
npx playwright test

# 单文件测试
npx playwright test e2e/drawing.spec.ts

# 生成 HTML 报告
npx playwright test --reporter=html

# 无头模式快速运行
npx playwright test --headed=false --workers=8
```
